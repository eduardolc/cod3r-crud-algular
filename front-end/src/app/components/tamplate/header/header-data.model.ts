export class HeaderData {
    title: string;
    icon: string;
    routeUrl: string;

    constructor(title: string, icon: string, routeUrl: string) {
        this.title = title;
        this.icon = icon;
        this.routeUrl = routeUrl;
    }
}